from datetime import datetime
from typing import Dict, Any, list

class AttendanceManager:
    def __init__ (self,  db=None)
        self.db = db

    def add_attendance(self, FirstName=str, LastName=str, Email=str + int, SchoolName=str,)
